# Optional. Pre-fills the Settings page with a default Google Sheet source.
# This is a "published to the web" CSV link — public by design, no credentials involved.
# The app runs fine without it (bundled demo data).
VITE_SHEET_CSV_URL=
