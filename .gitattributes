# Batch files must stay CRLF or cmd.exe can misparse them.
*.bat text eol=crlf
*.cmd text eol=crlf

# PowerShell and AutoHotkey are fine either way; keep them CRLF on Windows checkout.
*.ps1 text eol=crlf
*.ahk text eol=crlf
