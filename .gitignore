# Dependencies and build output
node_modules
dist
dist-ssr
*.local

# Logs
logs
*.log
npm-debug.log*

# Editors / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# Local session tooling
.claude/

# Never commit environment or credential files.
# (Google OAuth credentials were once leaked from this repo — keep these rules.)
.env
.env.*
!.env.example
credentials.json
token.json
*.pem
venv/
__pycache__/

# The parent-PC config holds the Apps Script capability URL — never commit it.
capture/config.ini

# Local e2e mock-backend state
.mock-data/

# Built distributable bundle (reproducible from capture/ — see capture/SETUP.md)
ScamGuard-parent-PC.zip
